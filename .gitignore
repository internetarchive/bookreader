/BookReader/
/BookReaderDev/
node_modules/
.DS_Store
coverage*/
.env

.vscode
CLAUDE.local.md

# Playwright (prototype branch)
tests/playwright/screenshots/
test-results/
playwright-report/

# PocketTTS model weights are fetched at runtime, never committed (~146MB)
*.onnx
pocket-tts-sample.wav

# Locally-served PocketTTS weights for browser tests (fetched at runtime, ~170MB)
BookReaderDemo/pocket-tts-models/
