# AGENTS

## Notes

- The root-level `BookReader/` directory contains build artifacts/bundled files, so likely don't want to run code searches/grep in there. Prefer searching under `src/` (and relevant `tests/`) for implementation logic.
